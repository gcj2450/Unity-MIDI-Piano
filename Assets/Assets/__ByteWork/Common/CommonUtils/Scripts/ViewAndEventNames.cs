using UnityEngine;
using System.Collections;

public enum EventNames
{
    ShowStartView,
    ShowEndView = 1,
    OnMusicSignCheck,
    OnMusicSignExit,
}

public enum ViewNames
{
    StartView,
    EndView,
}